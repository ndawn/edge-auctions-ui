import { Box } from '@mui/material';
import { styled } from '@mui/material/styles';

const Thumbs = styled(Box)({
  marginTop: '16px',
  width: '100%',
  height: '56px',
  whiteSpace: 'nowrap',
});

export default Thumbs;
